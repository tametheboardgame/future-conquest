name: Build Phase VIII-B4B

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  build:
    if: github.head_ref == 'agent/engineering-projects-viii-b4b'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/engineering-projects-viii-b4b
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply Phase VIII-B4B implementation
        shell: bash
        run: |
          python <<'PY'
          from pathlib import Path

          def replace(path, old, new, count=1):
              file = Path(path)
              text = file.read_text()
              found = text.count(old)
              if found < count:
                  raise SystemExit(f"Expected {count} occurrence(s) in {path}, found {found}: {old[:120]!r}")
              file.write_text(text.replace(old, new, count))

          types = Path('src/game/types.ts')
          text = types.read_text()
          text = text.replace(
              "export type InfrastructureIncidentCause = 'resistance' | 'enemy-interdiction' | 'combat';\n",
              "export type InfrastructureIncidentCause = 'resistance' | 'enemy-interdiction' | 'combat';\nexport type EngineeringAllocation = 25 | 50 | 75 | 100;\nexport type EngineeringProjectStatus = 'active' | 'completed' | 'cancelled';\n"
          )
          text = text.replace(
              "  status: 'ready' | 'moving' | 'attacking' | 'garrison' | 'recovering';\n",
              "  status: 'ready' | 'moving' | 'attacking' | 'garrison' | 'recovering' | 'engineering';\n"
          )
          text = text.replace(
              "export interface InfrastructureIncident {\n",
              "export interface EngineeringProject {\n  id: string;\n  routeId: string;\n  assignedTaskGroupId: string;\n  createdTurn: number;\n  startingCondition: number;\n  targetCondition: number;\n  progress: number;\n  allocation: EngineeringAllocation;\n  supplySpent: number;\n  status: EngineeringProjectStatus;\n  returnStatus: 'ready' | 'garrison';\n}\n\nexport interface InfrastructureIncident {\n"
          )
          text = text.replace("  version: 9;\n", "  version: 10;\n", 1)
          text = text.replace(
              "  infrastructureIncidents?: InfrastructureIncident[];\n",
              "  infrastructureIncidents?: InfrastructureIncident[];\n  engineeringProjects: EngineeringProject[];\n"
          )
          types.write_text(text)

          Path('src/game/engineering-projects.ts').write_text(r'''import { STRATEGIC_ROUTE_BY_ID, STRATEGIC_ROUTES } from './strategic-network-data';
import { routeCapacityModifierForCondition, routeStatusForCondition } from './infrastructure-disruption';
import { refreshSupplyNetwork } from './supply-network';
import type {
  EngineeringAllocation,
  EngineeringProject,
  GameEvent,
  GameState,
  StrategicRouteState,
  TaskGroup
} from './types';

export const ENGINEERING_ALLOCATIONS: EngineeringAllocation[] = [25, 50, 75, 100];

const clamp = (value: number, minimum: number, maximum: number) => Math.max(minimum, Math.min(maximum, value));

function appendEvent(state: GameState, text: string, tone: GameEvent['tone']): GameState {
  const event: GameEvent = {
    id: (state.events[0]?.id ?? 0) + 1,
    turn: state.turn,
    text,
    tone
  };
  return { ...state, events: [event, ...state.events].slice(0, 100) };
}

function validAllocation(value: unknown): value is EngineeringAllocation {
  return value === 25 || value === 50 || value === 75 || value === 100;
}

export function engineeringProjectDemand(project: Pick<EngineeringProject, 'allocation'>): number {
  return Math.max(3, Math.ceil(project.allocation / 8));
}

export function normaliseEngineeringProjects(value: unknown): EngineeringProject[] {
  if (!Array.isArray(value)) return [];
  return value.flatMap((candidate): EngineeringProject[] => {
    if (!candidate || typeof candidate !== 'object') return [];
    const project = candidate as Partial<EngineeringProject>;
    if (
      typeof project.id !== 'string'
      || typeof project.routeId !== 'string'
      || typeof project.assignedTaskGroupId !== 'string'
      || typeof project.createdTurn !== 'number'
      || typeof project.startingCondition !== 'number'
      || typeof project.targetCondition !== 'number'
      || typeof project.progress !== 'number'
      || !validAllocation(project.allocation)
      || typeof project.supplySpent !== 'number'
      || (project.status !== 'active' && project.status !== 'completed' && project.status !== 'cancelled')
      || (project.returnStatus !== 'ready' && project.returnStatus !== 'garrison')
    ) return [];
    return [{
      id: project.id,
      routeId: project.routeId,
      assignedTaskGroupId: project.assignedTaskGroupId,
      createdTurn: Math.max(1, Math.round(project.createdTurn)),
      startingCondition: clamp(Math.round(project.startingCondition), 0, 100),
      targetCondition: clamp(Math.round(project.targetCondition), 1, 100),
      progress: clamp(Math.round(project.progress), 0, 100),
      allocation: project.allocation,
      supplySpent: Math.max(0, Math.round(project.supplySpent)),
      status: project.status,
      returnStatus: project.returnStatus
    }];
  }).slice(0, 80);
}

export function normaliseEngineeringState(
  value: unknown,
  taskGroups: Record<string, TaskGroup>,
  routeStates: Record<string, StrategicRouteState>
): { projects: EngineeringProject[]; taskGroups: Record<string, TaskGroup> } {
  const projects = normaliseEngineeringProjects(value);
  const groups = structuredClone(taskGroups);
  for (const project of projects) {
    if (project.status !== 'active') continue;
    const route = STRATEGIC_ROUTE_BY_ID[project.routeId];
    const routeState = routeStates[project.routeId];
    const group = groups[project.assignedTaskGroupId];
    const validGroup = Boolean(group && !group.order && (group.status === 'ready' || group.status === 'garrison' || group.status === 'engineering'));
    if (!route || !routeState || routeState.condition >= project.targetCondition || !validGroup) {
      project.status = routeState?.condition >= project.targetCondition ? 'completed' : 'cancelled';
      if (group?.status === 'engineering') group.status = project.returnStatus;
      continue;
    }
    group.status = 'engineering';
  }
  return { projects, taskGroups: groups };
}

export function engineeringGroupsForRoute(state: GameState, routeId: string): TaskGroup[] {
  const route = STRATEGIC_ROUTE_BY_ID[routeId];
  if (!route) return [];
  const endpoints = [route.fromTerritoryId, route.toTerritoryId];
  const assigned = new Set(state.engineeringProjects.filter(project => project.status === 'active').map(project => project.assignedTaskGroupId));
  return Object.values(state.taskGroups)
    .filter(group => (
      group.personnel > 0
      && endpoints.includes(group.location)
      && !group.order
      && !assigned.has(group.id)
      && (group.status === 'ready' || group.status === 'garrison')
    ))
    .sort((a, b) => a.name.localeCompare(b.name));
}

export function repairableEngineeringRoutes(state: GameState) {
  const activeRoutes = new Set(state.engineeringProjects.filter(project => project.status === 'active').map(project => project.routeId));
  return STRATEGIC_ROUTES.filter(route => {
    const routeState = state.routeStates[route.id];
    const from = state.territories[route.fromTerritoryId];
    const to = state.territories[route.toTerritoryId];
    return Boolean(
      routeState
      && routeState.condition < 100
      && !activeRoutes.has(route.id)
      && from?.controller === 'player'
      && to?.controller === 'player'
      && from.occupation !== 'unsecured'
      && to.occupation !== 'unsecured'
      && engineeringGroupsForRoute(state, route.id).length
    );
  }).sort((a, b) => (state.routeStates[a.id].condition - state.routeStates[b.id].condition) || a.name.localeCompare(b.name));
}

function nextProjectId(state: GameState, routeId: string): string {
  let suffix = 1;
  let id = `ENG-${state.turn}-${routeId}-${suffix}`;
  while (state.engineeringProjects.some(project => project.id === id)) {
    suffix += 1;
    id = `ENG-${state.turn}-${routeId}-${suffix}`;
  }
  return id;
}

export function startEngineeringProject(
  state: GameState,
  routeId: string,
  groupId: string,
  allocation: EngineeringAllocation = 50
): GameState {
  if (state.status !== 'playing' || !validAllocation(allocation)) return state;
  const route = STRATEGIC_ROUTE_BY_ID[routeId];
  const routeState = state.routeStates[routeId];
  const group = state.taskGroups[groupId];
  if (!route || !routeState || routeState.condition >= 100 || !group) return state;
  if (state.engineeringProjects.some(project => project.status === 'active' && (project.routeId === routeId || project.assignedTaskGroupId === groupId))) return state;
  const from = state.territories[route.fromTerritoryId];
  const to = state.territories[route.toTerritoryId];
  if (
    from?.controller !== 'player'
    || to?.controller !== 'player'
    || from.occupation === 'unsecured'
    || to.occupation === 'unsecured'
    || !engineeringGroupsForRoute(state, routeId).some(candidate => candidate.id === groupId)
  ) return state;

  const taskGroups = structuredClone(state.taskGroups);
  const returnStatus = group.status === 'garrison' ? 'garrison' : 'ready';
  taskGroups[groupId].status = 'engineering';
  const project: EngineeringProject = {
    id: nextProjectId(state, routeId),
    routeId,
    assignedTaskGroupId: groupId,
    createdTurn: state.turn,
    startingCondition: routeState.condition,
    targetCondition: 100,
    progress: 0,
    allocation,
    supplySpent: 0,
    status: 'active',
    returnStatus
  };
  const projects = [project, ...state.engineeringProjects].slice(0, 80);
  return refreshSupplyNetwork(appendEvent(
    { ...state, taskGroups, engineeringProjects: projects },
    `${group.name} began engineering work on ${route.name} at ${allocation}% allocation.`,
    'neutral'
  ));
}

export function setEngineeringAllocation(state: GameState, projectId: string, allocation: EngineeringAllocation): GameState {
  if (!validAllocation(allocation)) return state;
  const projects = structuredClone(state.engineeringProjects);
  const project = projects.find(candidate => candidate.id === projectId && candidate.status === 'active');
  if (!project || project.allocation === allocation) return state;
  project.allocation = allocation;
  const route = STRATEGIC_ROUTE_BY_ID[project.routeId];
  return refreshSupplyNetwork(appendEvent(
    { ...state, engineeringProjects: projects },
    `${route?.name ?? project.routeId} engineering allocation changed to ${allocation}%.`,
    'neutral'
  ));
}

export function cancelEngineeringProject(state: GameState, projectId: string): GameState {
  const projects = structuredClone(state.engineeringProjects);
  const project = projects.find(candidate => candidate.id === projectId && candidate.status === 'active');
  if (!project) return state;
  const taskGroups = structuredClone(state.taskGroups);
  const group = taskGroups[project.assignedTaskGroupId];
  if (group?.status === 'engineering') group.status = project.returnStatus;
  project.status = 'cancelled';
  const route = STRATEGIC_ROUTE_BY_ID[project.routeId];
  return refreshSupplyNetwork(appendEvent(
    { ...state, taskGroups, engineeringProjects: projects },
    `Engineering work on ${route?.name ?? project.routeId} was cancelled.`,
    'warning'
  ));
}

export function resolveEngineeringProjects(state: GameState): GameState {
  if (!state.engineeringProjects.some(project => project.status === 'active')) return state;
  const projects = structuredClone(state.engineeringProjects);
  const taskGroups = structuredClone(state.taskGroups);
  const routeStates = structuredClone(state.routeStates);
  let next: GameState = { ...state, engineeringProjects: projects, taskGroups, routeStates };

  for (const project of projects.filter(candidate => candidate.status === 'active').sort((a, b) => a.id.localeCompare(b.id))) {
    const route = STRATEGIC_ROUTE_BY_ID[project.routeId];
    const routeState = routeStates[project.routeId];
    const group = taskGroups[project.assignedTaskGroupId];
    const from = route ? state.territories[route.fromTerritoryId] : undefined;
    const to = route ? state.territories[route.toTerritoryId] : undefined;
    const valid = Boolean(
      route
      && routeState
      && group
      && group.status === 'engineering'
      && !group.order
      && (group.location === route.fromTerritoryId || group.location === route.toTerritoryId)
      && from?.controller === 'player'
      && to?.controller === 'player'
      && from.occupation !== 'unsecured'
      && to.occupation !== 'unsecured'
    );
    if (!valid) {
      project.status = 'cancelled';
      if (group?.status === 'engineering') group.status = project.returnStatus;
      next = appendEvent(next, `Engineering project ${project.id} was abandoned after the assigned corridor or formation became unavailable.`, 'warning');
      continue;
    }

    const deliveryRatio = clamp((state.logistics.formationAllocations[group.id]?.ratio ?? 0) / 100, 0, 1);
    const maximumWork = project.allocation / 10;
    const work = deliveryRatio >= 0.15 ? Math.max(1, Math.floor(maximumWork * deliveryRatio)) : 0;
    project.supplySpent += Math.round(engineeringProjectDemand(project) * deliveryRatio);
    if (work <= 0) {
      if (state.turn % 3 === 0) next = appendEvent(next, `${route.name} repair work is stalled by inadequate logistics throughput.`, 'warning');
      continue;
    }

    routeState.condition = clamp(routeState.condition + work, 0, project.targetCondition);
    routeState.status = routeStatusForCondition(routeState.condition);
    routeState.capacityModifier = routeCapacityModifierForCondition(routeState.condition);
    const totalWork = Math.max(1, project.targetCondition - project.startingCondition);
    project.progress = clamp(Math.round((routeState.condition - project.startingCondition) / totalWork * 100), 0, 100);

    if (routeState.condition >= project.targetCondition) {
      project.status = 'completed';
      project.progress = 100;
      group.status = project.returnStatus;
      next = appendEvent(next, `${group.name} completed repairs on ${route.name}. The corridor is fully operational.`, 'good');
    }
  }

  return next;
}
''')

          replace(
              'src/game/infrastructure-disruption.ts',
              "function routeCapacityModifier(condition: number): number {\n",
              "export function routeCapacityModifierForCondition(condition: number): number {\n"
          )
          replace(
              'src/game/infrastructure-disruption.ts',
              "routeStates[routeId].capacityModifier = routeCapacityModifier(condition);",
              "routeStates[routeId].capacityModifier = routeCapacityModifierForCondition(condition);"
          )
          replace(
              'src/game/infrastructure-disruption.ts',
              "    current.capacityModifier = routeCapacityModifier(current.condition);",
              "    current.capacityModifier = routeCapacityModifierForCondition(current.condition);"
          )
          infra = Path('src/game/infrastructure-disruption.ts')
          text = infra.read_text()
          start = text.index('export function resolveInfrastructureRecovery')
          end = text.index('\nexport function resolveInfrastructureDisruption', start)
          replacement = r'''export function resolveInfrastructureRecovery(state: GameState): GameState {
  const routeStates = structuredClone(state.routeStates);
  const repaired: string[] = [];

  for (const route of STRATEGIC_ROUTES) {
    const current = routeStates[route.id];
    if (!current || current.condition >= 100) continue;
    const from = state.territories[route.fromTerritoryId];
    const to = state.territories[route.toTerritoryId];
    if (
      !from
      || !to
      || from.controller !== 'player'
      || to.controller !== 'player'
      || from.occupation !== 'administered'
      || to.occupation !== 'administered'
      || !from.supplied
      || !to.supplied
    ) continue;

    const beforeStatus = current.status;
    current.condition = clamp(current.condition + 1, 0, 100);
    current.status = routeStatusForCondition(current.condition);
    current.capacityModifier = routeCapacityModifierForCondition(current.condition);
    if (beforeStatus !== current.status || current.condition === 100) repaired.push(route.name);
  }

  if (!repaired.length) return { ...state, routeStates };
  return appendEvent(
    { ...state, routeStates },
    `Routine maintenance restored ${repaired.slice(0, 3).join(', ')}${repaired.length > 3 ? ` and ${repaired.length - 3} other corridor${repaired.length - 3 === 1 ? '' : 's'}` : ''}.`,
    'good'
  );
}
'''
          infra.write_text(text[:start] + replacement + text[end:])

          replace(
              'src/game/supply-network.ts',
              "  recovering: 1.2\n};",
              "  recovering: 1.2,\n  engineering: 1.08\n};"
          )
          replace(
              'src/game/supply-network.ts',
              "  recovering: 1.04\n};",
              "  recovering: 1.04,\n  engineering: 1.16\n};"
          )
          replace(
              'src/game/supply-network.ts',
              "  for (const group of Object.values(state.taskGroups).sort((a, b) => a.id.localeCompare(b.id))) {\n    if (group.personnel <= 0) continue;\n    requests.push({\n      id: `GROUP:${group.id}`,\n      kind: 'formation',\n      targetTerritoryId: group.location,\n      demand: formationSupplyDemand(group),\n      priority: formationPriority[group.status],\n      delivered: 0,\n      pathCounts: new Map()\n    });\n  }",
              "  for (const group of Object.values(state.taskGroups).sort((a, b) => a.id.localeCompare(b.id))) {\n    if (group.personnel <= 0) continue;\n    const engineeringProject = state.engineeringProjects.find(project => project.status === 'active' && project.assignedTaskGroupId === group.id);\n    const engineeringDemand = engineeringProject ? Math.max(3, Math.ceil(engineeringProject.allocation / 8)) : 0;\n    requests.push({\n      id: `GROUP:${group.id}`,\n      kind: 'formation',\n      targetTerritoryId: group.location,\n      demand: formationSupplyDemand(group) + engineeringDemand,\n      priority: engineeringProject ? Math.max(1.16, formationPriority[group.status]) : formationPriority[group.status],\n      delivered: 0,\n      pathCounts: new Map()\n    });\n  }"
          )

          replace(
              'src/game/strategic-response.ts',
              "import { normaliseInfrastructureIncidents } from './infrastructure-disruption';\n",
              "import { normaliseInfrastructureIncidents } from './infrastructure-disruption';\nimport { normaliseEngineeringState } from './engineering-projects';\n"
          )
          replace(
              'src/game/strategic-response.ts',
              "  | 'infrastructureIncidents';\n",
              "  | 'infrastructureIncidents'\n  | 'engineeringProjects';\n"
          )
          replace(
              'src/game/strategic-response.ts',
              "  infrastructureIncidents?: GameState['infrastructureIncidents'];\n",
              "  infrastructureIncidents?: GameState['infrastructureIncidents'];\n  engineeringProjects?: GameState['engineeringProjects'];\n"
          )
          replace(
              'src/game/strategic-response.ts',
              "  const taskGroups = normaliseTaskGroupOrderRoutes(state.taskGroups, routeStates, previousVersion >= 7);\n  const upgraded = {\n",
              "  const routedTaskGroups = normaliseTaskGroupOrderRoutes(state.taskGroups, routeStates, previousVersion >= 7);\n  const engineering = normaliseEngineeringState(state.engineeringProjects, routedTaskGroups, routeStates);\n  const upgraded = {\n"
          )
          replace('src/game/strategic-response.ts', "    version: 9,\n    taskGroups,\n", "    version: 10,\n    taskGroups: engineering.taskGroups,\n")
          replace(
              'src/game/strategic-response.ts',
              "    infrastructureIncidents: normaliseInfrastructureIncidents(state.infrastructureIncidents),\n",
              "    infrastructureIncidents: normaliseInfrastructureIncidents(state.infrastructureIncidents),\n    engineeringProjects: engineering.projects,\n"
          )

          replace(
              'src/game/engine.ts',
              "import { resolveInfrastructureDisruption, resolveInfrastructureRecovery } from './infrastructure-disruption';\n",
              "import { resolveInfrastructureDisruption, resolveInfrastructureRecovery } from './infrastructure-disruption';\nimport { resolveEngineeringProjects } from './engineering-projects';\n"
          )
          replace(
              'src/game/engine.ts',
              "const SAVE_KEY = 'future-conquest-slice-v0.9';\nconst LEGACY_V8_SAVE_KEY = 'future-conquest-slice-v0.8';\n",
              "const SAVE_KEY = 'future-conquest-slice-v0.10';\nconst LEGACY_V9_SAVE_KEY = 'future-conquest-slice-v0.9';\nconst LEGACY_V8_SAVE_KEY = 'future-conquest-slice-v0.8';\n"
          )
          replace('src/game/engine.ts', "    version: 9,\n", "    version: 10,\n", 1)
          replace(
              'src/game/engine.ts',
              "    infrastructureIncidents: [],\n    supply: 100,\n",
              "    infrastructureIncidents: [],\n    engineeringProjects: [],\n    supply: 100,\n"
          )
          replace(
              'src/game/engine.ts',
              "    infrastructureIncidents: structuredClone(state.infrastructureIncidents ?? []),\n    events: [...state.events]\n",
              "    infrastructureIncidents: structuredClone(state.infrastructureIncidents ?? []),\n    engineeringProjects: structuredClone(state.engineeringProjects),\n    events: [...state.events]\n"
          )
          replace(
              'src/game/engine.ts',
              "  next = resolveInfrastructureRecovery(next);\n  next = resolveInfrastructureDisruption(next);\n  next = resolveMovement(next);\n",
              "  next = resolveInfrastructureDisruption(next);\n  next = resolveInfrastructureRecovery(next);\n  next = resolveEngineeringProjects(next);\n  next = resolveMovement(next);\n"
          )
          replace(
              'src/game/engine.ts',
              "type LogisticsField = 'logistics';\n\ntype LegacyV8GameState = Omit<GameState, 'version' | 'infrastructureIncidents'> & { version: 8 };\ntype LegacyV7GameState = Omit<GameState, 'version' | LogisticsField | 'infrastructureIncidents'> & { version: 7 };\ntype LegacyV6GameState = Omit<GameState, 'version' | LogisticsField> & { version: 6 };\ntype LegacyV5GameState = Omit<GameState, 'version' | NetworkField | LogisticsField> & { version: 5 };\ntype LegacyV4GameState = Omit<GameState, 'version' | StrategicField | NetworkField | LogisticsField> & { version: 4 };\ntype LegacyV3GameState = Omit<GameState, 'version' | StrategicField | NetworkField | LogisticsField> & { version: 3 };\ntype LegacyGameState = Omit<GameState, 'version' | 'operations' | StrategicField | NetworkField | LogisticsField> & {",
              "type LogisticsField = 'logistics';\ntype EngineeringField = 'engineeringProjects';\n\ntype LegacyV9GameState = Omit<GameState, 'version' | EngineeringField> & { version: 9 };\ntype LegacyV8GameState = Omit<GameState, 'version' | 'infrastructureIncidents' | EngineeringField> & { version: 8 };\ntype LegacyV7GameState = Omit<GameState, 'version' | LogisticsField | 'infrastructureIncidents' | EngineeringField> & { version: 7 };\ntype LegacyV6GameState = Omit<GameState, 'version' | LogisticsField | EngineeringField> & { version: 6 };\ntype LegacyV5GameState = Omit<GameState, 'version' | NetworkField | LogisticsField | EngineeringField> & { version: 5 };\ntype LegacyV4GameState = Omit<GameState, 'version' | StrategicField | NetworkField | LogisticsField | EngineeringField> & { version: 4 };\ntype LegacyV3GameState = Omit<GameState, 'version' | StrategicField | NetworkField | LogisticsField | EngineeringField> & { version: 3 };\ntype LegacyGameState = Omit<GameState, 'version' | 'operations' | StrategicField | NetworkField | LogisticsField | EngineeringField> & {"
          )
          replace(
              'src/game/engine.ts',
              "      parsed.version === 9\n",
              "      parsed.version === 10\n"
          )
          replace(
              'src/game/engine.ts',
              "      && parsed.infrastructureIncidents\n    ) return upgradeStrategicState(parsed as GameState);\n  }\n\n  const v8 = localStorage.getItem(LEGACY_V8_SAVE_KEY);\n",
              "      && parsed.infrastructureIncidents\n      && parsed.engineeringProjects\n    ) return upgradeStrategicState(parsed as GameState);\n  }\n\n  const v9 = localStorage.getItem(LEGACY_V9_SAVE_KEY);\n  if (v9) {\n    const parsed = JSON.parse(v9) as Partial<LegacyV9GameState>;\n    if (\n      parsed.version === 9\n      && parsed.taskGroups\n      && parsed.enemyFormations\n      && parsed.operations\n      && parsed.mobilisations\n      && parsed.enemyOrders\n      && parsed.intelligenceReports\n      && parsed.routeStates\n      && parsed.logistics\n      && parsed.infrastructureIncidents\n    ) return upgradeStrategicState(parsed as LegacyV9GameState);\n  }\n\n  const v8 = localStorage.getItem(LEGACY_V8_SAVE_KEY);\n"
          )

          persistence = Path('src/game/persistence.ts')
          text = persistence.read_text()
          text = text.replace("future-conquest-slice-v0.9", "future-conquest-slice-v0.10", 2)
          text = text.replace(
              "export const LEGACY_V8_SAVE_KEY = 'future-conquest-slice-v0.8';\n",
              "export const LEGACY_V9_SAVE_KEY = 'future-conquest-slice-v0.9';\nexport const LEGACY_V8_SAVE_KEY = 'future-conquest-slice-v0.8';\n"
          )
          text = text.replace("  saveVersion: 9;\n", "  saveVersion: 10;\n")
          text = text.replace(
              "source: 'v9' | 'v8' | 'v7' | 'v6' | 'v5' | 'v4' | 'v3' | 'v2'",
              "source: 'v10' | 'v9' | 'v8' | 'v7' | 'v6' | 'v5' | 'v4' | 'v3' | 'v2'"
          )
          text = text.replace(
              "type LogisticsField = 'logistics';\n\ntype LegacyV8State = Omit<GameState, 'version' | 'infrastructureIncidents'> & { version: 8 };\ntype LegacyV7State = Omit<GameState, 'version' | LogisticsField | 'infrastructureIncidents'> & { version: 7 };\ntype LegacyV6State = Omit<GameState, 'version' | LogisticsField> & { version: 6 };\ntype LegacyV5State = Omit<GameState, 'version' | NetworkField | LogisticsField> & { version: 5 };\ntype LegacyV4State = Omit<GameState, 'version' | StrategicField | NetworkField | LogisticsField> & { version: 4 };\ntype LegacyV3State = Omit<GameState, 'version' | StrategicField | NetworkField | LogisticsField> & { version: 3 };\ntype LegacyV2State = Omit<GameState, 'version' | 'operations' | StrategicField | NetworkField | LogisticsField> & {",
              "type LogisticsField = 'logistics';\ntype EngineeringField = 'engineeringProjects';\n\ntype LegacyV9State = Omit<GameState, 'version' | EngineeringField> & { version: 9 };\ntype LegacyV8State = Omit<GameState, 'version' | 'infrastructureIncidents' | EngineeringField> & { version: 8 };\ntype LegacyV7State = Omit<GameState, 'version' | LogisticsField | 'infrastructureIncidents' | EngineeringField> & { version: 7 };\ntype LegacyV6State = Omit<GameState, 'version' | LogisticsField | EngineeringField> & { version: 6 };\ntype LegacyV5State = Omit<GameState, 'version' | NetworkField | LogisticsField | EngineeringField> & { version: 5 };\ntype LegacyV4State = Omit<GameState, 'version' | StrategicField | NetworkField | LogisticsField | EngineeringField> & { version: 4 };\ntype LegacyV3State = Omit<GameState, 'version' | StrategicField | NetworkField | LogisticsField | EngineeringField> & { version: 3 };\ntype LegacyV2State = Omit<GameState, 'version' | 'operations' | StrategicField | NetworkField | LogisticsField | EngineeringField> & {"
          )
          text = text.replace("function isV9State(value: unknown): value is GameState {\n", "function isV10State(value: unknown): value is GameState {\n")
          text = text.replace(
              "    && value.version === 9\n    && hasStrategicCollections(value)\n    && isRecord(value.routeStates)\n    && isRecord(value.logistics)\n    && Array.isArray(value.infrastructureIncidents);\n}\n\nfunction isV8State",
              "    && value.version === 10\n    && hasStrategicCollections(value)\n    && isRecord(value.routeStates)\n    && isRecord(value.logistics)\n    && Array.isArray(value.infrastructureIncidents)\n    && Array.isArray(value.engineeringProjects);\n}\n\nfunction isV9State(value: unknown): value is LegacyV9State {\n  return hasCoreCampaignState(value)\n    && value.version === 9\n    && hasStrategicCollections(value)\n    && isRecord(value.routeStates)\n    && isRecord(value.logistics)\n    && Array.isArray(value.infrastructureIncidents);\n}\n\nfunction isV8State"
          )
          text = text.replace("    saveVersion: 9,\n", "    saveVersion: 10,\n")
          text = text.replace("    && value.saveVersion === 9\n", "    && value.saveVersion === 10\n")
          text = text.replace(
              "function inspectRaw(storage: StorageReader, raw: string, source: 'v9' | 'v8' | 'v7' | 'v6' | 'v5' | 'v4' | 'v3' | 'v2'): SaveInspection {",
              "function inspectRaw(storage: StorageReader, raw: string, source: 'v10' | 'v9' | 'v8' | 'v7' | 'v6' | 'v5' | 'v4' | 'v3' | 'v2'): SaveInspection {"
          )
          text = text.replace(
              "    if (source === 'v9' && isV9State(parsed)) {\n      return { ok: true, state: upgradeStrategicState(parsed), metadata: readMetadata(storage, parsed), source };\n    }\n    if (source === 'v8'",
              "    if (source === 'v10' && isV10State(parsed)) {\n      return { ok: true, state: upgradeStrategicState(parsed), metadata: readMetadata(storage, parsed), source };\n    }\n    if (source === 'v9' && isV9State(parsed)) {\n      const state = upgradeStrategicState(parsed);\n      return { ok: true, state, metadata: createSaveMetadata(state, null), source };\n    }\n    if (source === 'v8'"
          )
          text = text.replace("  if (current) return inspectRaw(storage, current, 'v9');\n\n  const v8", "  if (current) return inspectRaw(storage, current, 'v10');\n\n  const v9 = readRaw(storage, LEGACY_V9_SAVE_KEY);\n  if (typeof v9 !== 'string' && v9 !== null) return v9;\n  if (v9) return inspectRaw(storage, v9, 'v9');\n\n  const v8")
          text = text.replace("    if (!isV9State(parsed))", "    if (!isV10State(parsed))")
          persistence.write_text(text)

          replace(
              'src/components/CommandNavigation.tsx',
              "export type CommandView = 'map' | 'forces' | 'operations' | 'territories' | 'intelligence' | 'campaign';",
              "export type CommandView = 'map' | 'forces' | 'operations' | 'territories' | 'engineering' | 'intelligence' | 'campaign';"
          )
          replace(
              'src/components/CommandNavigation.tsx',
              "  { id: 'territories', code: 'TER', label: 'Territories' },\n  { id: 'intelligence', code: 'INT', label: 'Intelligence' },",
              "  { id: 'territories', code: 'TER', label: 'Territories' },\n  { id: 'engineering', code: 'ENG', label: 'Engineering' },\n  { id: 'intelligence', code: 'INT', label: 'Intelligence' },"
          )

          Path('src/components/EngineeringCommand.tsx').write_text(r'''import { useState } from 'react';
import { TERRITORIES } from '../game/data';
import {
  cancelEngineeringProject,
  ENGINEERING_ALLOCATIONS,
  engineeringGroupsForRoute,
  engineeringProjectDemand,
  repairableEngineeringRoutes,
  setEngineeringAllocation,
  startEngineeringProject
} from '../game/engineering-projects';
import { STRATEGIC_ROUTE_BY_ID } from '../game/strategic-network-data';
import type { EngineeringAllocation, GameState } from '../game/types';

interface Props {
  state: GameState;
  onChange: (state: GameState | ((current: GameState) => GameState)) => void;
  onOpenTerritory: (territoryId: string) => void;
}

export function EngineeringCommand({ state, onChange, onOpenTerritory }: Props) {
  const [routeSelection, setRouteSelection] = useState('');
  const [groupSelection, setGroupSelection] = useState('');
  const [allocation, setAllocation] = useState<EngineeringAllocation>(50);

  const activeProjects = state.engineeringProjects.filter(project => project.status === 'active');
  const projectHistory = state.engineeringProjects.filter(project => project.status !== 'active').slice(0, 8);
  const repairableRoutes = repairableEngineeringRoutes(state);
  const selectedRouteId = repairableRoutes.some(route => route.id === routeSelection)
    ? routeSelection
    : repairableRoutes[0]?.id ?? '';
  const selectedRoute = selectedRouteId ? STRATEGIC_ROUTE_BY_ID[selectedRouteId] : undefined;
  const eligibleGroups = selectedRoute ? engineeringGroupsForRoute(state, selectedRoute.id) : [];
  const selectedGroupId = eligibleGroups.some(group => group.id === groupSelection)
    ? groupSelection
    : eligibleGroups[0]?.id ?? '';
  const damagedRouteCount = Object.values(state.routeStates).filter(route => route.condition < 100).length;
  const allocatedEffort = activeProjects.reduce((sum, project) => sum + project.allocation, 0);
  const dailyDemand = activeProjects.reduce((sum, project) => sum + engineeringProjectDemand(project), 0);

  return <section className="command-view engineering-view">
    <header className="command-view-header"><div><p className="panel-label">ENGINEERING</p><h2>Infrastructure repair command</h2></div><p>Assign formations and logistics effort to damaged strategic corridors. Higher allocation repairs faster but consumes more network throughput.</p></header>

    <div className="engineering-summary-strip">
      <div><span>Damaged corridors</span><strong>{damagedRouteCount}</strong></div>
      <div><span>Active projects</span><strong>{activeProjects.length}</strong></div>
      <div><span>Allocated effort</span><strong>{allocatedEffort}%</strong></div>
      <div><span>Daily repair demand</span><strong>{dailyDemand}</strong></div>
    </div>

    <div className="engineering-command-grid">
      <section className="view-panel engineering-create-panel">
        <div className="view-panel-heading"><p className="panel-label">NEW REPAIR PROJECT</p><strong>{repairableRoutes.length}</strong></div>
        {selectedRoute ? <>
          <label>Damaged corridor
            <select value={selectedRouteId} onChange={event => { setRouteSelection(event.target.value); setGroupSelection(''); }}>
              {repairableRoutes.map(route => <option key={route.id} value={route.id}>{route.name} · {Math.round(state.routeStates[route.id].condition)}%</option>)}
            </select>
          </label>
          <label>Assigned formation
            <select value={selectedGroupId} onChange={event => setGroupSelection(event.target.value)}>
              {eligibleGroups.map(group => <option key={group.id} value={group.id}>{group.name} · {TERRITORIES[group.location].centre} · {group.status}</option>)}
            </select>
          </label>
          <label>Engineering allocation
            <select value={allocation} onChange={event => setAllocation(Number(event.target.value) as EngineeringAllocation)}>
              {ENGINEERING_ALLOCATIONS.map(value => <option key={value} value={value}>{value}% · {engineeringProjectDemand({ allocation: value })} daily demand</option>)}
            </select>
          </label>
          <div className="engineering-route-preview">
            <strong>{selectedRoute.name}</strong>
            <span>{TERRITORIES[selectedRoute.fromTerritoryId].centre} → {TERRITORIES[selectedRoute.toTerritoryId].centre}</span>
            <div className="engineering-progress"><i style={{ width: `${state.routeStates[selectedRoute.id].condition}%` }} /></div>
            <small>Current condition {Math.round(state.routeStates[selectedRoute.id].condition)}%. Assigned formations cannot move or attack while engineering work is active.</small>
          </div>
          <button className="primary" disabled={!selectedGroupId} onClick={() => onChange(current => startEngineeringProject(current, selectedRoute.id, selectedGroupId, allocation))}>Start repair project</button>
        </> : <div className="view-empty"><h3>No repair project available</h3><p>Every controlled corridor is either fully operational, already assigned or inaccessible to a ready formation.</p></div>}
      </section>

      <section className="view-panel engineering-project-panel">
        <div className="view-panel-heading"><p className="panel-label">ACTIVE PROJECTS</p><strong>{activeProjects.length}</strong></div>
        {activeProjects.length ? <div className="engineering-project-list">{activeProjects.map(project => {
          const route = STRATEGIC_ROUTE_BY_ID[project.routeId];
          const group = state.taskGroups[project.assignedTaskGroupId];
          const routeState = state.routeStates[project.routeId];
          const allocation = state.logistics.formationAllocations[project.assignedTaskGroupId];
          return <article key={project.id} className="engineering-project-card">
            <header><div><small>{project.id}</small><h3>{route?.name ?? project.routeId}</h3></div><b>{project.progress}%</b></header>
            <div className="engineering-progress"><i style={{ width: `${project.progress}%` }} /></div>
            <dl>
              <div><dt>Assigned formation</dt><dd>{group?.name ?? project.assignedTaskGroupId}</dd></div>
              <div><dt>Route condition</dt><dd>{Math.round(routeState?.condition ?? 0)}%</dd></div>
              <div><dt>Supply delivery</dt><dd>{allocation?.ratio ?? 0}%</dd></div>
              <div><dt>Supply spent</dt><dd>{project.supplySpent}</dd></div>
            </dl>
            <label>Repair allocation
              <select value={project.allocation} onChange={event => onChange(current => setEngineeringAllocation(current, project.id, Number(event.target.value) as EngineeringAllocation))}>
                {ENGINEERING_ALLOCATIONS.map(value => <option key={value} value={value}>{value}% · demand {engineeringProjectDemand({ allocation: value })}</option>)}
              </select>
            </label>
            <div className="engineering-project-actions">
              {route && <button onClick={() => onOpenTerritory(route.fromTerritoryId)}>Open corridor</button>}
              <button className="danger-action" onClick={() => onChange(current => cancelEngineeringProject(current, project.id))}>Cancel project</button>
            </div>
          </article>;
        })}</div> : <p className="empty-state">No formation is currently committed to infrastructure repair.</p>}
      </section>

      <section className="view-panel engineering-history-panel">
        <div className="view-panel-heading"><p className="panel-label">PROJECT HISTORY</p><strong>{projectHistory.length}</strong></div>
        {projectHistory.length ? <div className="engineering-history-list">{projectHistory.map(project => {
          const route = STRATEGIC_ROUTE_BY_ID[project.routeId];
          return <article key={project.id} className={project.status}><span><strong>{route?.name ?? project.routeId}</strong><small>Started day {String(project.createdTurn).padStart(3, '0')} · {project.supplySpent} supply spent</small></span><b>{project.status}</b></article>;
        })}</div> : <p className="empty-state">No engineering projects have yet been completed or cancelled.</p>}
      </section>
    </div>
  </section>;
}
''')

          replace(
              'src/App.tsx',
              "import { FormationRoster } from './components/FormationRoster';\n",
              "import { FormationRoster } from './components/FormationRoster';\nimport { EngineeringCommand } from './components/EngineeringCommand';\n"
          )
          replace(
              'src/App.tsx',
              "  const selectedOperation = selectedGroup ? getOperationForGroup(state, selectedGroup.id) : undefined;\n",
              "  const selectedOperation = selectedGroup ? getOperationForGroup(state, selectedGroup.id) : undefined;\n  const selectedEngineeringProject = selectedGroup ? state.engineeringProjects.find(project => project.status === 'active' && project.assignedTaskGroupId === selectedGroup.id) : undefined;\n"
          )
          replace(
              'src/App.tsx',
              "    if (selectedGroup.order?.type === 'move') return `${selectedGroup.name} is moving towards ${TERRITORIES[selectedGroup.order.target].centre}. Other formations may still receive orders before the day resolves.`;\n",
              "    if (selectedEngineeringProject) return `${selectedGroup.name} is repairing ${STRATEGIC_ROUTE_BY_ID[selectedEngineeringProject.routeId]?.name ?? selectedEngineeringProject.routeId} at ${selectedEngineeringProject.allocation}% allocation.`;\n    if (selectedGroup.order?.type === 'move') return `${selectedGroup.name} is moving towards ${TERRITORIES[selectedGroup.order.target].centre}. Other formations may still receive orders before the day resolves.`;\n"
          )
          replace(
              'src/App.tsx',
              "  }, [chosenRoute, selectedGroup, selectedOperation, target, targetInfo, targetOperation]);\n",
              "  }, [chosenRoute, selectedEngineeringProject, selectedGroup, selectedOperation, target, targetInfo, targetOperation]);\n"
          )
          replace(
              'src/App.tsx',
              "<p className=\"eyebrow\">PHASE VIII-B4A / INFRASTRUCTURE DISRUPTION</p>",
              "<p className=\"eyebrow\">PHASE VIII-B4B / ENGINEERING PROJECTS</p>"
          )
          replace(
              'src/App.tsx',
              "badges={{ forces: groups.length, operations: operations.length, territories: `${controlled}/${territoryDefinitions.length}`, intelligence: frontlineTerritories.length }}",
              "badges={{ forces: groups.length, operations: operations.length, territories: `${controlled}/${territoryDefinitions.length}`, engineering: state.engineeringProjects.filter(project => project.status === 'active').length, intelligence: frontlineTerritories.length }}"
          )
          replace(
              'src/App.tsx',
              "        {currentView === 'intelligence' && <section className=\"command-view intelligence-view\">",
              "        {currentView === 'engineering' && <EngineeringCommand state={state} onChange={setState} onOpenTerritory={openTerritoryOnMap} />}\n\n        {currentView === 'intelligence' && <section className=\"command-view intelligence-view\">"
          )
          replace(
              'src/App.tsx',
              "                <div><dt>Route bottlenecks</dt><dd>{bottleneckRoutes.length}</dd></div>\n",
              "                <div><dt>Route bottlenecks</dt><dd>{bottleneckRoutes.length}</dd></div>\n                <div><dt>Engineering projects</dt><dd>{state.engineeringProjects.filter(project => project.status === 'active').length}</dd></div>\n"
          )

          Path('src/engineering.css').write_text(r'''.engineering-summary-strip {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 0.75rem;
  margin-bottom: 1rem;
}

.engineering-summary-strip > div,
.engineering-route-preview,
.engineering-project-card,
.engineering-history-list article {
  border: 1px solid rgba(118, 153, 167, 0.28);
  background: rgba(8, 22, 29, 0.72);
}

.engineering-summary-strip > div {
  padding: 0.85rem;
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.engineering-summary-strip span,
.engineering-project-card dt,
.engineering-history-list small {
  color: var(--muted);
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.engineering-summary-strip strong {
  font-size: 1.35rem;
}

.engineering-command-grid {
  display: grid;
  grid-template-columns: minmax(270px, 0.75fr) minmax(360px, 1.25fr);
  gap: 1rem;
  align-items: start;
}

.engineering-create-panel,
.engineering-project-panel,
.engineering-history-panel {
  display: flex;
  flex-direction: column;
  gap: 0.85rem;
}

.engineering-history-panel {
  grid-column: 1 / -1;
}

.engineering-create-panel label,
.engineering-project-card label {
  display: grid;
  gap: 0.35rem;
  color: var(--muted);
  font-size: 0.78rem;
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.engineering-route-preview {
  padding: 0.9rem;
  display: grid;
  gap: 0.45rem;
}

.engineering-route-preview span,
.engineering-route-preview small {
  color: var(--muted);
}

.engineering-progress {
  height: 0.48rem;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.08);
}

.engineering-progress i {
  display: block;
  height: 100%;
  background: linear-gradient(90deg, #55a9bf, #8ed6b0);
}

.engineering-project-list {
  display: grid;
  gap: 0.75rem;
}

.engineering-project-card {
  padding: 0.9rem;
  display: grid;
  gap: 0.75rem;
}

.engineering-project-card header,
.engineering-project-actions,
.engineering-history-list article {
  display: flex;
  justify-content: space-between;
  gap: 0.75rem;
  align-items: center;
}

.engineering-project-card header small {
  color: var(--muted);
}

.engineering-project-card h3 {
  margin: 0.15rem 0 0;
}

.engineering-project-card dl {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.55rem;
  margin: 0;
}

.engineering-project-card dl > div {
  display: flex;
  justify-content: space-between;
  gap: 0.5rem;
  border-bottom: 1px solid rgba(118, 153, 167, 0.16);
  padding-bottom: 0.35rem;
}

.engineering-project-card dd {
  margin: 0;
}

.engineering-history-list {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.65rem;
}

.engineering-history-list article {
  padding: 0.75rem;
}

.engineering-history-list span {
  display: grid;
  gap: 0.2rem;
}

.engineering-history-list article.completed b {
  color: #8ed6b0;
}

.engineering-history-list article.cancelled b {
  color: #e49c8c;
}

@media (max-width: 980px) {
  .engineering-command-grid,
  .engineering-summary-strip,
  .engineering-history-list {
    grid-template-columns: 1fr;
  }

  .engineering-history-panel {
    grid-column: auto;
  }
}
''')
          replace(
              'src/main.tsx',
              "import './persistence-feedback';\n",
              "import './persistence-feedback';\nimport './engineering.css';\n"
          )

          for path in Path('tests').glob('*.test.cjs'):
              text = path.read_text()
              text = text.replace('assert.equal(state.version, 9);', 'assert.equal(state.version, 10);')
              text = text.replace('assert.equal(loaded.version, 9);', 'assert.equal(loaded.version, 10);')
              text = text.replace('assert.equal(upgraded.version, 9);', 'assert.equal(upgraded.version, 10);')
              text = text.replace('assert.equal(result.state.version, 9);', 'assert.equal(result.state.version, 10);')
              text = text.replace('saveVersion: 9,', 'saveVersion: 10,')
              text = text.replace('saveVersion:\\s*9', 'saveVersion:\\s*10')
              text = text.replace('future-conquest-slice-v0\\.9', 'future-conquest-slice-v0\\.10')
              text = text.replace('PHASE VIII-B4A \\/ INFRASTRUCTURE DISRUPTION', 'PHASE VIII-B4B \\/ ENGINEERING PROJECTS')
              text = text.replace('PHASE VIII-B4A / INFRASTRUCTURE DISRUPTION', 'PHASE VIII-B4B / ENGINEERING PROJECTS')
              path.write_text(text)

          persistence_test = Path('tests/persistence.test.cjs')
          text = persistence_test.read_text()
          text = text.replace("  LEGACY_V8_SAVE_KEY,\n", "  LEGACY_V9_SAVE_KEY,\n  LEGACY_V8_SAVE_KEY,\n")
          text = text.replace('a current version 9 save is inspected with its matching metadata', 'a current version 10 save is inspected with its matching metadata')
          text = text.replace("assert.equal(result.source, 'v9');", "assert.equal(result.source, 'v10');", 1)
          persistence_test.write_text(text)

          Path('tests/engineering-projects-viii-b4b.test.cjs').write_text(r'''const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const { newGame } = require('../.test-dist/engine.js');
const {
  cancelEngineeringProject,
  engineeringProjectDemand,
  resolveEngineeringProjects,
  setEngineeringAllocation,
  startEngineeringProject
} = require('../.test-dist/engineering-projects.js');
const { refreshSupplyNetwork } = require('../.test-dist/supply-network.js');
const { upgradeStrategicState } = require('../.test-dist/strategic-response.js');
const { LEGACY_V9_SAVE_KEY, inspectStoredCampaign } = require('../.test-dist/persistence.js');

const read = file => fs.readFileSync(path.join(process.cwd(), file), 'utf8');

function engineeringState() {
  let state = structuredClone(newGame(711, 'standard'));
  state.portalTerritory = 'BE-01';
  for (const id of ['BE-01', 'NL-01']) {
    state.territories[id].controller = 'player';
    state.territories[id].occupation = 'administered';
    state.territories[id].supplied = true;
    state.territories[id].resistance = 10;
  }
  state.taskGroups['TG-1'].location = 'BE-01';
  state.taskGroups['TG-1'].status = 'ready';
  state.taskGroups['TG-1'].order = undefined;
  state.routeStates['R-BRUSSELS-AMSTERDAM'].condition = 55;
  state.routeStates['R-BRUSSELS-AMSTERDAM'].status = 'damaged';
  state.routeStates['R-BRUSSELS-AMSTERDAM'].capacityModifier = 0.64;
  return refreshSupplyNetwork(state);
}

function installStorage(entries = []) {
  const values = new Map(entries);
  return {
    getItem: key => values.get(key) ?? null,
    setItem: (key, value) => values.set(key, value),
    removeItem: key => values.delete(key)
  };
}

test('new campaigns initialise version 10 engineering state', () => {
  const state = newGame(711, 'standard');
  assert.equal(state.version, 10);
  assert.deepEqual(state.engineeringProjects, []);
});

test('starting a repair project commits a formation and adds logistics demand', () => {
  const state = engineeringState();
  const beforeDemand = state.logistics.formationAllocations['TG-1'].demand;
  const started = startEngineeringProject(state, 'R-BRUSSELS-AMSTERDAM', 'TG-1', 50);
  assert.equal(started.engineeringProjects.length, 1);
  assert.equal(started.engineeringProjects[0].status, 'active');
  assert.equal(started.taskGroups['TG-1'].status, 'engineering');
  assert.ok(started.logistics.formationAllocations['TG-1'].demand > beforeDemand);
  assert.equal(engineeringProjectDemand(started.engineeringProjects[0]), 7);
});

test('repair allocation changes logistics demand without creating another project', () => {
  const started = startEngineeringProject(engineeringState(), 'R-BRUSSELS-AMSTERDAM', 'TG-1', 25);
  const projectId = started.engineeringProjects[0].id;
  const lowDemand = started.logistics.formationAllocations['TG-1'].demand;
  const urgent = setEngineeringAllocation(started, projectId, 100);
  assert.equal(urgent.engineeringProjects.length, 1);
  assert.equal(urgent.engineeringProjects[0].allocation, 100);
  assert.ok(urgent.logistics.formationAllocations['TG-1'].demand > lowDemand);
});

test('supplied engineering work restores route condition and releases the formation on completion', () => {
  let state = engineeringState();
  state.routeStates['R-BRUSSELS-AMSTERDAM'].condition = 96;
  state = startEngineeringProject(state, 'R-BRUSSELS-AMSTERDAM', 'TG-1', 100);
  const resolved = resolveEngineeringProjects(state);
  assert.equal(resolved.routeStates['R-BRUSSELS-AMSTERDAM'].condition, 100);
  assert.equal(resolved.routeStates['R-BRUSSELS-AMSTERDAM'].status, 'open');
  assert.equal(resolved.engineeringProjects[0].status, 'completed');
  assert.equal(resolved.taskGroups['TG-1'].status, 'ready');
});

test('cancelling a project releases a formation to its prior garrison status', () => {
  const state = engineeringState();
  state.taskGroups['TG-1'].status = 'garrison';
  const started = startEngineeringProject(state, 'R-BRUSSELS-AMSTERDAM', 'TG-1', 50);
  const cancelled = cancelEngineeringProject(started, started.engineeringProjects[0].id);
  assert.equal(cancelled.engineeringProjects[0].status, 'cancelled');
  assert.equal(cancelled.taskGroups['TG-1'].status, 'garrison');
});

test('version 9 campaigns migrate to version 10 with an empty project ledger', () => {
  const legacy = structuredClone(newGame(711, 'standard'));
  legacy.version = 9;
  delete legacy.engineeringProjects;
  const upgraded = upgradeStrategicState(legacy);
  assert.equal(upgraded.version, 10);
  assert.deepEqual(upgraded.engineeringProjects, []);

  const result = inspectStoredCampaign(installStorage([[LEGACY_V9_SAVE_KEY, JSON.stringify(legacy)]]));
  assert.equal(result.ok, true);
  assert.equal(result.source, 'v9');
  assert.equal(result.state.version, 10);
});

test('the interface exposes engineering command and the Phase VIII-B4B release marker', () => {
  const app = read('src/App.tsx');
  const nav = read('src/components/CommandNavigation.tsx');
  const engineering = read('src/components/EngineeringCommand.tsx');
  const persistence = read('src/game/persistence.ts');
  assert.match(app, /PHASE VIII-B4B \/ ENGINEERING PROJECTS/);
  assert.match(app, /currentView === 'engineering'/);
  assert.match(nav, /id: 'engineering'/);
  assert.match(engineering, /Higher allocation repairs faster but consumes more network throughput/);
  assert.match(persistence, /future-conquest-slice-v0\.10/);
  assert.match(persistence, /saveVersion:\s*10/);
});
''')

          Path('docs/design/phase-08b4b-engineering-projects.md').write_text(r'''# Phase VIII-B4B — Engineering Projects and Repair Allocation

Version 10 gives the player direct control over strategic infrastructure repair.

## Engineering projects

- A repair project can be created for a damaged route whose endpoints are both controlled and sufficiently secured.
- A ready or garrison formation must be located at either end of the corridor.
- The assigned formation enters engineering status and cannot move, attack or reorganise until the project completes or is cancelled.
- Completed and cancelled projects remain in a persistent project history.

## Repair allocation

Each project can be assigned 25%, 50%, 75% or 100% engineering intensity.

Higher intensity:

- increases daily route-condition recovery;
- increases the assigned formation's logistics demand;
- can place additional pressure on bottleneck routes and other formations.

Actual repair work is reduced when the assigned formation receives inadequate logistics throughput. Projects can stall completely when delivery falls below the critical threshold.

## Passive maintenance

Automatic route recovery is now limited to one condition point per day and only applies when both endpoint territories are administered and supplied. Significant damage therefore requires an explicit engineering project.

## Persistence

Campaign saves advance to version 10. Version 9 and all earlier supported saves migrate with an empty engineering-project ledger.

## Interface

A dedicated Engineering command view provides:

- damaged-corridor selection;
- eligible formation assignment;
- repair-intensity control;
- live project progress, route condition, supply delivery and resources spent;
- cancellation and project history.

## Scope boundary

Deliberate player interdiction missions, combat-generated route damage and logistics-priority controls remain for later Phase VIII-B4 increments.
''')
          PY

      - name: Install dependencies
        run: npm ci

      - name: Run regression suite
        run: npm test

      - name: Build production bundle
        run: npm run build

      - name: Commit implementation
        shell: bash
        run: |
          rm .github/workflows/build-viii-b4b.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Add engineering projects and repair allocation"
          git push origin HEAD:agent/engineering-projects-viii-b4b
