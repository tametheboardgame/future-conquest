name: Refine compact map confirmation

on:
  pull_request:
    branches:
      - main
    types: [synchronize, reopened]

permissions:
  contents: write

jobs:
  refine:
    if: github.event.pull_request.head.ref == 'agent/compact-map-confirmation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/compact-map-confirmation
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply layer refinement
        run: python3 scripts/refine-compact-map-confirmation.py

      - name: Install dependencies
        run: npm ci

      - name: Run regression suite
        run: npm test

      - name: Build production bundle
        run: npm run build

      - name: Commit validated refinement
        shell: bash
        run: |
          set -euo pipefail
          rm -f scripts/refine-compact-map-confirmation.py .github/workflows/refine-compact-map-confirmation.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "fix: keep map confirmation above selection layers"
          git push origin HEAD:agent/compact-map-confirmation
