name: Build Phase VIII-B4A

on:
  push:
    branches:
      - agent/infrastructure-disruption-viii-b4a

permissions:
  contents: write

jobs:
  build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/infrastructure-disruption-viii-b4a
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply Phase VIII-B4A implementation
        shell: bash
        run: |
          python <<'PY'
          from pathlib import Path

          def replace(path, old, new, count=1):
              file = Path(path)
              text = file.read_text()
              found = text.count(old)
              if found < count:
                  raise SystemExit(f"Expected {count} occurrence(s) in {path}, found {found}: {old[:100]!r}")
              file.write_text(text.replace(old, new, count))

          types = Path('src/game/types.ts')
          text = types.read_text()
          text = text.replace(
              "export type RouteSupplyCondition = 'idle' | 'active' | 'strained' | 'overloaded';\n",
              "export type RouteSupplyCondition = 'idle' | 'active' | 'strained' | 'overloaded';\nexport type InfrastructureIncidentCause = 'resistance' | 'enemy-interdiction' | 'combat';\n"
          )
          text = text.replace(
              "export interface GameEvent {\n",
              "export interface InfrastructureIncident {\n  id: string;\n  turn: number;\n  routeId: string;\n  cause: InfrastructureIncidentCause;\n  severity: number;\n  description: string;\n}\n\nexport interface GameEvent {\n"
          )
          text = text.replace("  version: 8;\n", "  version: 9;\n", 1)
          text = text.replace(
              "  logistics: LogisticsState;\n",
              "  logistics: LogisticsState;\n  infrastructureIncidents?: InfrastructureIncident[];\n"
          )
          types.write_text(text)

          Path('src/game/infrastructure-disruption.ts').write_text(r'''import { TERRITORIES } from './data';
import { STRATEGIC_ROUTES } from './strategic-network-data';
import type {
  GameEvent,
  GameState,
  InfrastructureIncident,
  InfrastructureIncidentCause,
  StrategicRouteState
} from './types';

const clamp = (value: number, minimum: number, maximum: number) => Math.max(minimum, Math.min(maximum, value));

const randomFor = (seed: number, turn: number, salt: number) => {
  let value = (seed + turn * 99991 + salt * 7919) >>> 0;
  value = (value * 1664525 + 1013904223) >>> 0;
  return value / 4294967296;
};

const saltFor = (value: string) => [...value].reduce(
  (sum, character, index) => sum + character.charCodeAt(0) * (index + 1),
  0
);

export function routeStatusForCondition(condition: number): StrategicRouteState['status'] {
  if (condition <= 0) return 'destroyed';
  if (condition < 35) return 'blocked';
  if (condition < 75) return 'damaged';
  return 'open';
}

function routeCapacityModifier(condition: number): number {
  if (condition <= 0) return 0;
  return Math.round(clamp(0.2 + condition / 125, 0.2, 1) * 100) / 100;
}

export function normaliseInfrastructureIncidents(value: unknown): InfrastructureIncident[] {
  if (!Array.isArray(value)) return [];
  return value.filter((incident): incident is InfrastructureIncident => Boolean(
    incident
    && typeof incident === 'object'
    && typeof incident.id === 'string'
    && typeof incident.turn === 'number'
    && typeof incident.routeId === 'string'
    && (incident.cause === 'resistance' || incident.cause === 'enemy-interdiction' || incident.cause === 'combat')
    && typeof incident.severity === 'number'
    && typeof incident.description === 'string'
  )).slice(0, 80);
}

function appendEvent(state: GameState, text: string, tone: GameEvent['tone']): GameState {
  const event: GameEvent = {
    id: (state.events[0]?.id ?? 0) + 1,
    turn: state.turn,
    text,
    tone
  };
  return { ...state, events: [event, ...state.events].slice(0, 100) };
}

export function applyInfrastructureDamage(
  state: GameState,
  routeId: string,
  severity: number,
  cause: InfrastructureIncidentCause
): GameState {
  const route = STRATEGIC_ROUTES.find(candidate => candidate.id === routeId);
  const current = state.routeStates[routeId];
  if (!route || !current || severity <= 0) return state;

  const routeStates = structuredClone(state.routeStates);
  const beforeStatus = routeStates[routeId].status;
  const condition = clamp(Math.round(routeStates[routeId].condition - severity), 0, 100);
  routeStates[routeId].condition = condition;
  routeStates[routeId].status = routeStatusForCondition(condition);
  routeStates[routeId].capacityModifier = routeCapacityModifier(condition);

  const causeLabel = cause === 'resistance'
    ? 'Resistance sabotage'
    : cause === 'enemy-interdiction'
      ? 'Enemy interdiction'
      : 'Combat damage';
  const description = `${causeLabel} damaged ${route.name}, reducing corridor condition to ${condition}%.`;
  const incident: InfrastructureIncident = {
    id: `INF-${state.turn}-${routeId}-${(state.infrastructureIncidents?.length ?? 0) + 1}`,
    turn: state.turn,
    routeId,
    cause,
    severity: Math.round(severity),
    description
  };
  const incidents = [incident, ...(state.infrastructureIncidents ?? [])].slice(0, 80);
  const statusChange = beforeStatus !== routeStates[routeId].status
    ? ` The corridor is now ${routeStates[routeId].status}.`
    : '';
  return appendEvent({ ...state, routeStates, infrastructureIncidents: incidents }, `${description}${statusChange}`, condition < 35 ? 'danger' : 'warning');
}

export function resolveInfrastructureRecovery(state: GameState): GameState {
  const routeStates = structuredClone(state.routeStates);
  const repaired: string[] = [];

  for (const route of STRATEGIC_ROUTES) {
    const current = routeStates[route.id];
    if (!current || current.condition >= 100) continue;
    const from = state.territories[route.fromTerritoryId];
    const to = state.territories[route.toTerritoryId];
    if (!from || !to || from.controller !== 'player' || to.controller !== 'player') continue;
    if (from.occupation === 'unsecured' || to.occupation === 'unsecured') continue;

    const securedEndpoints = [from, to].filter(territory => territory.occupation === 'administered').length;
    const suppliedEndpoints = [from, to].filter(territory => territory.supplied).length;
    const engineeringPresence = Object.values(state.taskGroups).some(group =>
      group.personnel > 0
      && (group.location === route.fromTerritoryId || group.location === route.toTerritoryId)
      && (group.status === 'ready' || group.status === 'garrison')
    );
    const repair = 1 + securedEndpoints + suppliedEndpoints + (engineeringPresence ? 1 : 0);
    const beforeStatus = current.status;
    current.condition = clamp(current.condition + repair, 0, 100);
    current.status = routeStatusForCondition(current.condition);
    current.capacityModifier = routeCapacityModifier(current.condition);
    if (beforeStatus !== current.status || current.condition === 100) repaired.push(route.name);
  }

  if (!repaired.length) return { ...state, routeStates };
  return appendEvent(
    { ...state, routeStates },
    `Infrastructure crews restored ${repaired.slice(0, 3).join(', ')}${repaired.length > 3 ? ` and ${repaired.length - 3} other corridor${repaired.length - 3 === 1 ? '' : 's'}` : ''}.`,
    'good'
  );
}

export function resolveInfrastructureDisruption(state: GameState): GameState {
  const candidates = STRATEGIC_ROUTES.flatMap(route => {
    const from = state.territories[route.fromTerritoryId];
    const to = state.territories[route.toTerritoryId];
    if (!from || !to) return [];
    const playerEndpoints = [from, to].filter(territory => territory.controller === 'player').length;
    if (!playerEndpoints) return [];

    let cause: InfrastructureIncidentCause;
    let risk: number;
    if (playerEndpoints === 2) {
      const resistance = Math.max(from.resistance, to.resistance);
      if (resistance < 18) return [];
      cause = 'resistance';
      risk = resistance / 720;
    } else {
      cause = 'enemy-interdiction';
      risk = 0.025 + state.escalation / 950;
    }

    const protectedRoute = Object.values(state.taskGroups).some(group =>
      group.status === 'garrison'
      && group.personnel >= 500
      && (group.location === route.fromTerritoryId || group.location === route.toTerritoryId)
    );
    if (protectedRoute) risk *= 0.55;
    const roll = randomFor(state.seed, state.turn, saltFor(route.id) + (cause === 'resistance' ? 401 : 809));
    return roll < risk ? [{ route, cause, margin: risk - roll }] : [];
  }).sort((a, b) => b.margin - a.margin || a.route.id.localeCompare(b.route.id));

  const selected = candidates[0];
  if (!selected) return state;
  const severityRoll = randomFor(state.seed, state.turn, saltFor(selected.route.id) + 1301);
  const severity = 8 + Math.floor(severityRoll * 17) + (selected.cause === 'enemy-interdiction' ? Math.floor(state.escalation / 25) : 0);
  return applyInfrastructureDamage(state, selected.route.id, severity, selected.cause);
}
''')

          replace(
              'src/game/strategic-response.ts',
              "import { refreshSupplyNetwork } from './supply-network';\n",
              "import { refreshSupplyNetwork } from './supply-network';\nimport { normaliseInfrastructureIncidents } from './infrastructure-disruption';\n"
          )
          replace(
              'src/game/strategic-response.ts',
              "  | 'routeStates'\n  | 'logistics';\n",
              "  | 'routeStates'\n  | 'logistics'\n  | 'infrastructureIncidents';\n"
          )
          replace(
              'src/game/strategic-response.ts',
              "  logistics?: GameState['logistics'];\n",
              "  logistics?: GameState['logistics'];\n  infrastructureIncidents?: GameState['infrastructureIncidents'];\n"
          )
          replace(
              'src/game/strategic-response.ts',
              "    version: 8,\n    taskGroups,\n    routeStates,\n",
              "    version: 9,\n    taskGroups,\n    routeStates,\n    infrastructureIncidents: normaliseInfrastructureIncidents(state.infrastructureIncidents),\n"
          )

          replace(
              'src/game/engine.ts',
              "import { createEmptyLogisticsState, refreshSupplyNetwork } from './supply-network';\n",
              "import { createEmptyLogisticsState, refreshSupplyNetwork } from './supply-network';\nimport { resolveInfrastructureDisruption, resolveInfrastructureRecovery } from './infrastructure-disruption';\n"
          )
          replace(
              'src/game/engine.ts',
              "const SAVE_KEY = 'future-conquest-slice-v0.8';\nconst LEGACY_V7_SAVE_KEY = 'future-conquest-slice-v0.7';\n",
              "const SAVE_KEY = 'future-conquest-slice-v0.9';\nconst LEGACY_V8_SAVE_KEY = 'future-conquest-slice-v0.8';\nconst LEGACY_V7_SAVE_KEY = 'future-conquest-slice-v0.7';\n"
          )
          replace('src/game/engine.ts', "    version: 8,\n", "    version: 9,\n", 1)
          replace(
              'src/game/engine.ts',
              "    logistics: createEmptyLogisticsState(1),\n    supply: 100,\n",
              "    logistics: createEmptyLogisticsState(1),\n    infrastructureIncidents: [],\n    supply: 100,\n"
          )
          replace(
              'src/game/engine.ts',
              "    logistics: structuredClone(state.logistics),\n    events: [...state.events]\n",
              "    logistics: structuredClone(state.logistics),\n    infrastructureIncidents: structuredClone(state.infrastructureIncidents ?? []),\n    events: [...state.events]\n"
          )
          replace(
              'src/game/engine.ts',
              "  next = resolveOccupationAndLogistics(next);\n  next = resolveStrategicResponse(next);\n  next = resolveCounterattack(next);\n",
              "  next = resolveOccupationAndLogistics(next);\n  next = resolveInfrastructureRecovery(next);\n  next = resolveStrategicResponse(next);\n  next = resolveCounterattack(next);\n  next = resolveInfrastructureDisruption(next);\n"
          )
          replace(
              'src/game/engine.ts',
              "type LegacyV7GameState = Omit<GameState, 'version' | LogisticsField> & { version: 7 };\n",
              "type LegacyV8GameState = Omit<GameState, 'version' | 'infrastructureIncidents'> & { version: 8 };\ntype LegacyV7GameState = Omit<GameState, 'version' | LogisticsField | 'infrastructureIncidents'> & { version: 7 };\n"
          )
          replace(
              'src/game/engine.ts',
              "      parsed.version === 8\n      && parsed.taskGroups\n",
              "      parsed.version === 9\n      && parsed.taskGroups\n"
          )
          replace(
              'src/game/engine.ts',
              "      && parsed.routeStates\n    ) return upgradeStrategicState(parsed as GameState);\n  }\n\n  const v7 = localStorage.getItem(LEGACY_V7_SAVE_KEY);\n",
              "      && parsed.routeStates\n      && parsed.infrastructureIncidents\n    ) return upgradeStrategicState(parsed as GameState);\n  }\n\n  const v8 = localStorage.getItem(LEGACY_V8_SAVE_KEY);\n  if (v8) {\n    const parsed = JSON.parse(v8) as Partial<LegacyV8GameState>;\n    if (\n      parsed.version === 8\n      && parsed.taskGroups\n      && parsed.enemyFormations\n      && parsed.operations\n      && parsed.mobilisations\n      && parsed.enemyOrders\n      && parsed.intelligenceReports\n      && parsed.routeStates\n      && parsed.logistics\n    ) return upgradeStrategicState(parsed as LegacyV8GameState);\n  }\n\n  const v7 = localStorage.getItem(LEGACY_V7_SAVE_KEY);\n"
          )

          persistence = Path('src/game/persistence.ts')
          text = persistence.read_text()
          text = text.replace("future-conquest-slice-v0.8", "future-conquest-slice-v0.9", 2)
          text = text.replace(
              "export const LEGACY_V7_SAVE_KEY = 'future-conquest-slice-v0.7';\n",
              "export const LEGACY_V8_SAVE_KEY = 'future-conquest-slice-v0.8';\nexport const LEGACY_V7_SAVE_KEY = 'future-conquest-slice-v0.7';\n"
          )
          text = text.replace("  saveVersion: 8;\n", "  saveVersion: 9;\n")
          text = text.replace(
              "source: 'v8' | 'v7' | 'v6' | 'v5' | 'v4' | 'v3' | 'v2'",
              "source: 'v9' | 'v8' | 'v7' | 'v6' | 'v5' | 'v4' | 'v3' | 'v2'"
          )
          text = text.replace(
              "type LegacyV7State = Omit<GameState, 'version' | LogisticsField> & { version: 7 };\n",
              "type LegacyV8State = Omit<GameState, 'version' | 'infrastructureIncidents'> & { version: 8 };\ntype LegacyV7State = Omit<GameState, 'version' | LogisticsField | 'infrastructureIncidents'> & { version: 7 };\n"
          )
          text = text.replace("function isV8State(value: unknown): value is GameState {\n", "function isV9State(value: unknown): value is GameState {\n")
          text = text.replace(
              "    && value.version === 8\n    && hasStrategicCollections(value)\n    && isRecord(value.routeStates)\n    && isRecord(value.logistics);\n}\n\nfunction isV7State",
              "    && value.version === 9\n    && hasStrategicCollections(value)\n    && isRecord(value.routeStates)\n    && isRecord(value.logistics)\n    && Array.isArray(value.infrastructureIncidents);\n}\n\nfunction isV8State(value: unknown): value is LegacyV8State {\n  return hasCoreCampaignState(value)\n    && value.version === 8\n    && hasStrategicCollections(value)\n    && isRecord(value.routeStates)\n    && isRecord(value.logistics);\n}\n\nfunction isV7State"
          )
          text = text.replace("    saveVersion: 8,\n", "    saveVersion: 9,\n")
          text = text.replace("    && value.saveVersion === 8\n", "    && value.saveVersion === 9\n")
          text = text.replace(
              "function inspectRaw(storage: StorageReader, raw: string, source: 'v8' | 'v7' | 'v6' | 'v5' | 'v4' | 'v3' | 'v2'): SaveInspection {",
              "function inspectRaw(storage: StorageReader, raw: string, source: 'v9' | 'v8' | 'v7' | 'v6' | 'v5' | 'v4' | 'v3' | 'v2'): SaveInspection {"
          )
          text = text.replace(
              "    if (source === 'v8' && isV8State(parsed)) {\n      return { ok: true, state: upgradeStrategicState(parsed), metadata: readMetadata(storage, parsed), source };\n    }\n    if (source === 'v7'",
              "    if (source === 'v9' && isV9State(parsed)) {\n      return { ok: true, state: upgradeStrategicState(parsed), metadata: readMetadata(storage, parsed), source };\n    }\n    if (source === 'v8' && isV8State(parsed)) {\n      const state = upgradeStrategicState(parsed);\n      return { ok: true, state, metadata: createSaveMetadata(state, null), source };\n    }\n    if (source === 'v7'"
          )
          text = text.replace("  if (current) return inspectRaw(storage, current, 'v8');\n\n  const v7", "  if (current) return inspectRaw(storage, current, 'v9');\n\n  const v8 = readRaw(storage, LEGACY_V8_SAVE_KEY);\n  if (typeof v8 !== 'string' && v8 !== null) return v8;\n  if (v8) return inspectRaw(storage, v8, 'v8');\n\n  const v7")
          text = text.replace("    if (!isV8State(parsed))", "    if (!isV9State(parsed))")
          persistence.write_text(text)

          app = Path('src/App.tsx')
          app_text = app.read_text().replace('PHASE VIII-B3 / SUPPLY THROUGHPUT', 'PHASE VIII-B4A / INFRASTRUCTURE DISRUPTION')
          app.write_text(app_text)

          test = Path('tests/supply-throughput-viii-b3.test.cjs')
          test_text = test.read_text()
          test_text = test_text.replace("assert.equal(state.version, 8);", "assert.equal(state.version, 9);")
          test_text = test_text.replace("version 7 campaigns migrate to version 8", "version 7 campaigns migrate through version 8 to version 9")
          test_text = test_text.replace("assert.equal(upgraded.version, 8);", "assert.equal(upgraded.version, 9);")
          test_text = test_text.replace("PHASE VIII-B3 \\/ SUPPLY THROUGHPUT", "PHASE VIII-B4A \\/ INFRASTRUCTURE DISRUPTION")
          test_text = test_text.replace("future-conquest-slice-v0\\.8", "future-conquest-slice-v0\\.9")
          test_text = test_text.replace("saveVersion:\\s*8", "saveVersion:\\s*9")
          test.write_text(test_text)

          Path('tests/infrastructure-disruption-viii-b4a.test.cjs').write_text(r'''const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const { newGame } = require('../.test-dist/engine.js');
const {
  applyInfrastructureDamage,
  resolveInfrastructureRecovery,
  routeStatusForCondition
} = require('../.test-dist/infrastructure-disruption.js');
const { upgradeStrategicState } = require('../.test-dist/strategic-response.js');

const read = file => fs.readFileSync(path.join(process.cwd(), file), 'utf8');

test('new campaigns initialise the version 9 infrastructure incident ledger', () => {
  const state = newGame(711, 'standard');
  assert.equal(state.version, 9);
  assert.deepEqual(state.infrastructureIncidents, []);
});

test('infrastructure damage persists condition, status, capacity and incident history', () => {
  const state = newGame(711, 'standard');
  const damaged = applyInfrastructureDamage(state, 'R-PARIS-STRASBOURG', 70, 'enemy-interdiction');
  assert.equal(damaged.routeStates['R-PARIS-STRASBOURG'].condition, 30);
  assert.equal(damaged.routeStates['R-PARIS-STRASBOURG'].status, 'blocked');
  assert.ok(damaged.routeStates['R-PARIS-STRASBOURG'].capacityModifier < 1);
  assert.equal(damaged.infrastructureIncidents.length, 1);
  assert.equal(damaged.infrastructureIncidents[0].cause, 'enemy-interdiction');
  assert.match(damaged.events[0].text, /Enemy interdiction damaged/);
});

test('secured and supplied player corridors recover gradually', () => {
  const state = structuredClone(newGame(711, 'standard'));
  for (const id of ['BE-01', 'NL-01']) {
    state.territories[id].controller = 'player';
    state.territories[id].occupation = 'administered';
    state.territories[id].supplied = true;
  }
  state.routeStates['R-BRUSSELS-AMSTERDAM'].condition = 40;
  state.routeStates['R-BRUSSELS-AMSTERDAM'].status = 'damaged';
  state.routeStates['R-BRUSSELS-AMSTERDAM'].capacityModifier = 0.52;
  const recovered = resolveInfrastructureRecovery(state);
  assert.ok(recovered.routeStates['R-BRUSSELS-AMSTERDAM'].condition > 40);
  assert.ok(recovered.routeStates['R-BRUSSELS-AMSTERDAM'].capacityModifier > 0.52);
});

test('route condition thresholds produce explicit operational states', () => {
  assert.equal(routeStatusForCondition(100), 'open');
  assert.equal(routeStatusForCondition(60), 'damaged');
  assert.equal(routeStatusForCondition(20), 'blocked');
  assert.equal(routeStatusForCondition(0), 'destroyed');
});

test('version 8 campaigns migrate to version 9 with an empty incident ledger', () => {
  const legacy = structuredClone(newGame(711, 'standard'));
  legacy.version = 8;
  delete legacy.infrastructureIncidents;
  const upgraded = upgradeStrategicState(legacy);
  assert.equal(upgraded.version, 9);
  assert.deepEqual(upgraded.infrastructureIncidents, []);
});

test('the source exposes the Phase VIII-B4A release and disruption engine', () => {
  const app = read('src/App.tsx');
  const engine = read('src/game/engine.ts');
  const persistence = read('src/game/persistence.ts');
  const disruption = read('src/game/infrastructure-disruption.ts');
  assert.match(app, /PHASE VIII-B4A \/ INFRASTRUCTURE DISRUPTION/);
  assert.match(engine, /resolveInfrastructureDisruption/);
  assert.match(engine, /future-conquest-slice-v0\.9/);
  assert.match(persistence, /saveVersion:\s*9/);
  assert.match(disruption, /Resistance sabotage/);
  assert.match(disruption, /Enemy interdiction/);
});
''')

          Path('docs/design/phase-08b4a-infrastructure-disruption.md').write_text(r'''# Phase VIII-B4A — Infrastructure Disruption

Version 9 introduces persistent damage to the strategic route network.

## Disruption model

- Resistance can sabotage routes inside occupied territory, with risk driven by local resistance.
- Enemy forces can interdict frontier corridors, with risk increasing as escalation rises.
- A substantial garrison at either end reduces disruption risk.
- At most one deterministic infrastructure incident is selected each campaign day.
- Every incident records its cause, route, severity, turn and description.

## Route consequences

Route condition now drives persistent operational state:

- 75–100: open
- 35–74: damaged
- 1–34: blocked
- 0: destroyed

Damage also recalculates route capacity modifiers, immediately affecting movement and supply throughput.

## Recovery baseline

Secured player-to-player corridors recover gradually. Administered and supplied endpoints accelerate work, while a ready or garrison formation provides an additional local engineering presence bonus.

## Persistence

Campaign saves advance to version 9. Version 8 and all earlier supported saves migrate with an empty infrastructure incident ledger while retaining route condition and logistics state.

## Scope boundary

This increment establishes persistent disruption and baseline recovery. Player-issued engineering projects, repair resource allocation, deliberate interdiction missions, combat-generated route damage and manual logistics priorities follow in later Phase VIII-B4 increments.
''')

          # Guard against stale release markers elsewhere in active tests.
          for path in Path('tests').glob('*.test.cjs'):
              text = path.read_text()
              text = text.replace("assert.equal(state.version, 8);", "assert.equal(state.version, 9);")
              text = text.replace("assert.equal(upgraded.version, 8);", "assert.equal(upgraded.version, 9);")
              path.write_text(text)
          PY

      - name: Install dependencies
        run: npm ci

      - name: Run regression suite
        run: npm test

      - name: Build production bundle
        run: npm run build

      - name: Commit implementation
        shell: bash
        run: |
          rm .github/workflows/build-viii-b4a.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Add infrastructure disruption and recovery"
          git push origin HEAD:agent/infrastructure-disruption-viii-b4a
